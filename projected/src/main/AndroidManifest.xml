<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Android Auto gates car data behind Google Play services permissions.
      These are distinct from the AAOS platform permissions (android.car.permission.*)
      used by the :automotive module — same data, different gatekeeper.
    -->
    <uses-permission android:name="com.google.android.gms.permission.CAR_FUEL" />
    <uses-permission android:name="com.google.android.gms.permission.CAR_SPEED" />
    <uses-permission android:name="com.google.android.gms.permission.CAR_MILEAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_info_details"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.DayNight">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!--
          IOT is the closest permitted category for a vehicle-state app. There is no
          "diagnostics" or "dashboard" category, and publishing under a category that
          does not match the app is the usual cause of the Play review rejection
          "Auto App Quality Guidelines: Category not permitted".
          For sideloaded development this only affects which host surfaces show the app.
        -->
        <service
            android:name=".ProbeCarAppService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.IOT" />
            </intent-filter>
        </service>

        <activity
            android:name=".PhoneActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
